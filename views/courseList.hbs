{{>header}}

<div class="d-flex justify-content-center">
   <h1>Acciones adicionales del coordinador</h1>
</div>

<div class="container">

   <h2>Lista de estudiantes por Curso</h2>

   <table class="table">
      <thead class="thead-dark">
         <tr>
            <th>Nombre del curso</th>
            <th>Descripción del curso</th>
            <th>Precio del curso</th>
            <th>Activo</th>
            <th>Estudiantes</th>
         </tr>
      </thead>
      <tbody>
         {{#each courses}}
         <tr>
            <td>{{this.courseName}}</td>
            <td>{{this.courseDescription}}</td>
            <td>{{this.coursePrice}}</td>
            <td>{{this.active}}</td>
            <td> 
               <a href="/studentsByCourse/{{this.courseID}}" style="margin-right: 2%;">Ver estudiantes</a>
               <a href="/changeState/{{this.courseID}}">Cambiar de estado</a>
            </td>
         </tr>
         {{/each}}
      </tbody>
   </table>
</div>

{{>footer}}